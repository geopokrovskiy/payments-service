providers:
  fake-provider:
    methods:
      top-up:
        provider-url:
          host: localhost
          port: 8083
          uri: /api/v1/payments/top_up
        notification-url:
          host: localhost
          port: 8089
          uri: /api/v1/webhooks/payment-provider/fake-provider
      pay-out:
        provider-url:
          host: localhost
          port: 8083
          uri: /api/v1/payments/pay_out
        notification-url:
          host: localhost
          port: 8089
          uri: /api/v1/webhooks/payment-provider/fake-provider
